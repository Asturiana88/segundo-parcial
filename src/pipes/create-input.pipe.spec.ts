import { CreateInputPipe } from './create-input.pipe';

describe('CreateInputPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateInputPipe();
    expect(pipe).toBeTruthy();
  });
});
